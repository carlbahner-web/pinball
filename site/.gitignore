# Not committed: the paper grain and distress sheets, mirrored by
# fetch-assets.sh, plus the two brand faces. The logo, MGMT lockup and BUZZ
# artwork ARE committed, so the page renders anywhere.
fonts/
assets/website-noise*.webp
assets/plate-*.png
