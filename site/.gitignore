# Not committed: the distress sheets for Tier-2 weathering, and the two brand
# faces. The logo, MGMT lockup, BUZZ and the paper grain ARE committed, so the
# page renders anywhere. fetch-assets.sh mirrors the rest.
fonts/
assets/plate-*.png
