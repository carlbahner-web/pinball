# The distress sheets for Tier-2 weathering are the only thing not committed.
# Everything the page needs to render — artwork, the paper grain, and both
# brand faces — is in the repo, so it runs from a bare clone with no fetch
# step. The faces are the same files the live StudioLand site already serves
# publicly at /fonts/, so committing them exposes nothing new.
assets/plate-*.png
